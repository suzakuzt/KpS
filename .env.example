# Use mock for UI-only testing, gateway for real image generation.
IMAGE_PROVIDER=gateway

# Optional external Responses-compatible gateway.
# Leave unset when using the Codex login on the server.
# CODEX_RESPONSES_BASE_URL=https://your-gateway.example/v1
# OPENAI_API_KEY=your_gateway_key
